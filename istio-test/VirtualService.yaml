apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: istio-test-virtualservice
spec:
  hosts:
  - "*"
  gateways:
  - istio-test-gateway
  http:
  - route:
    - destination:
        host: istio-test